package org.grash

class Tokens {

    static final WHITE = '[ ]+'
    static final SYM = '[a-zA-Z0-9]+'
    static final REGEXP = '[^:]+'
    static final VAL = '[^:]+'
    static final PATH = '[^:]+'
    static final ACTION = /(prm|seq|alt|set)\(.+\)/
	
}

